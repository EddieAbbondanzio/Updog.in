/**
 * Mutations to set data in the vuex store for spaces.
 */
export enum SpaceMutation {
    SetSubscribed = 'setSubscribed',
    SetDefault = 'setDefault',
    ClearSubscribed = 'clearSubscribed'
}
