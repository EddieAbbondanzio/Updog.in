namespace Blurtle.Application {
    /// <summary>
    /// Various statuses of auth tokens.
    /// </summary>
    public enum AuthenticationTokenStatus {
        Valid,
        Expired,
        Invalid
    }
}