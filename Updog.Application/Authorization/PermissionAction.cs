namespace Updog.Application {
    public enum PermissionAction {
        CreatePost,
        ReadPost,
        UpdatePost,
        DeletePost,
        CreateComment,
        ReadComment,
        UpdateComment,
        DeleteComment
    }
}