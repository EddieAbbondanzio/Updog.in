namespace Updog.Domain {
    /// <summary>
    /// Direction of the vote. Either to the moon, or down.
    /// </summary>
    public enum VoteDirection {
        Up = 1,
        Neutral = 0,
        Down = -1
    }
}